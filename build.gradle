defaultTasks 'build','protocolbuffers:generateClasses', 'flatbuffers:generateClasses', 'sbe:generateClasses'

allprojects {
    group 'transficc'
    version '1.0-SNAPSHOT'

}

subprojects {
    apply plugin: 'java'
    repositories {
        mavenCentral()
    }
}

